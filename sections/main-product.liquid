<h1>{{ product.title }}</h1>
<img src="{{ product.featured_image | image_url }}">
<br>
<h3>Price: {{ product.price }}</h3>

{% schema %}
  {
    "name": "Product",
    "settings": []
  }
{% endschema %}